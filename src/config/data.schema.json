{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Local Data",
  "description": "Path for local data",
  "type": "object",
  "properties": {
    "skills": {
      "description": "Path for skills data",
      "type": "string"
    },
    "sections": {
      "description": "Path for sections data",
      "type": "string"
    }
  },
  "required": [
    "skills",
    "sections"
  ]
}