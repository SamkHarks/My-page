{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Local data",
  "description": "Api for local data",
  "type": "object",
  "properties": {
    "skills": {
      "description": "path for fetching skills data",
      "type": "string"
    },
    "sections": {
      "description": "Path for fetching sections data",
      "type": "string"
    }
  },
  "required": [
    "skills",
    "sections"
  ]
}