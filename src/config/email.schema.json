{
  "$schema": "http://json-schema.org/draft-07/schema",
  "title": "Email configuration",
  "description": "Api for sending emails",
  "type": "object",
  "properties": {
    "contact": {
      "description": "Path for posting email",
      "type": "string"
    },
    "health": {
      "description": "Path for health check",
      "type": "string"
    }
  },
  "required": [
    "contact",
    "health"
  ]
}